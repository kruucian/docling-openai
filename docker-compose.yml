version: "3.9"
services:
  docling-serve:
    build: .
    ports:
      - "5001:5001"
    environment:
      # Enable UI if desired
      - DOCLING_SERVE_ENABLE_UI=1
      # Configure OpenAI defaults (optional)
      - OPENAI_MODEL=gpt-4o-mini
    volumes:
      - ./data:/data  # scratch / results
